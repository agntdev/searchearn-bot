[
  {
    "name": "Approve Withdrawal without ID shows error",
    "steps": [
      {
        "send": { "callback": "admin:approve_withdrawal" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No pending withdrawal specified." }
          }
        ]
      }
    ]
  }
]
