[
  {
    "name": "Reject Withdrawal without ID shows error",
    "steps": [
      {
        "send": { "callback": "admin:reject_withdrawal" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "No pending withdrawal specified." }
          }
        ]
      }
    ]
  }
]
