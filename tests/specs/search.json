[
  {
    "name": "/search prompts for a query",
    "steps": [
      {
        "send": { "text": "/search" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": { "text": "Prompt for search query and return results" }
          }
        ]
      }
    ]
  }
]
